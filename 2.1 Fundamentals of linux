Operating System:
why?
 - interation of hardware resorces like cpu and memeoary.
 - so, we need os to help these resolve
 - need gui or cli for interacting hardware resorces.

OS:
 - Special software program that knows how to interact with hardware.
 - process maangement.
 - Memoary management.
 - device management.
 - Network management.
 - provides CLI and gui
 - ex: linux, macOS and windows
 - 1960 -> unix was the first os, very difficult for developers to interact with hardware using unix.
 - 1970 - >minix was on top of unix, not fully open source
 - 1980 -> MicroSoft windows -> GUI
 - 1990 -> lins developed Linux kernel, Open source
 - in 2025 90% of workloads linux opensource community developers, always secure .

Strcture Of Linux:
 - Kernel 
   - engine of the os   

User app or cli[vim, docker, Apache]
|
|
Shell[bash,zsh,fish] -> CLI
|
|
System packages [glibc, libc, OpenSSL]
|
|
Kernel [process, memoary, File system, network]
|
|
Hardware [cpu, RAM, Disk, Network, Peripherals]


Linx distributions
 - UBANTU, REDHAT, DEBIAN, ALPINE
 - Linux is OpenSource take copy and add more feture to them.
 - distribute them as a product
 - UBANTU is very popular and free

How to setup LInux
 - intall wsl on windows wsl --install
 - run wsl command
 - Docker can used for setup vm on virtal machine

docker run -dit `
  --name ubuntu-container `
  --hostname ubuntu-dev `
  --restart unless-stopped `
  --cpus="2" `
  --memory="4g" `
  --mount type=bind,source="C:/Users/Monica Korla/Downloads/ubuntu-container",target=/data `
  -v /var/run/docker.sock:/var/run/docker.sock `
  -p 2222:22 `
  -p 8080:80 `
  --env TZ=Asia/Kolkata `
  --env LANG=en_US.UTF-8 `
  ubuntu:latest /bin/bash  

 docker exec -it <container-id> /bin/bash 

you docker will run as virtual machine on either windows or macOs  

What is Package Manager?
  - install python
  - Linux distribution do add package and maintain the version 

apt list command:
luffy@luffy-kafka-server:~$ apt list
Listing... Done
0ad-data-common/noble 0.0.26-1 all
0ad-data/noble 0.0.26-1 all
0ad/noble-updates 0.0.26-6ubuntu0.24.04.1 arm64
0install-core/noble 2.18-2ubuntu2 arm64
0install/noble 2.18-2ubuntu2 arm64
0xffff/noble 0.9-1 arm64

luffy@luffy-kafka-server:~$ apt
apt 2.8.3 (arm64)
Usage: apt [options] command

apt is a commandline package manager and provides commands for
searching and managing as well as querying information about packages.
It provides the same functionality as the specialized APT tools,
like apt-get and apt-cache, but enables options more suitable for
interactive use by default.

Most used commands:
  list - list packages based on package names
  search - search in package descriptions
  show - show package details
  install - install packages
  reinstall - reinstall packages
  remove - remove packages
  autoremove - automatically remove all unused packages
  update - update list of available packages
  upgrade - upgrade the system by installing/upgrading packages
  full-upgrade - upgrade the system by removing/installing/upgrading packages
  edit-sources - edit the source information file
  satisfy - satisfy dependency strings

See apt(8) for more information about the available commands.
Configuration options and syntax is detailed in apt.conf(5).
Information about how to configure sources can be found in sources.list(5).
Package and version choices can be expressed via apt_preferences(5).
Security details are available in apt-secure(8).
                                        This APT has Super Cow Powers.